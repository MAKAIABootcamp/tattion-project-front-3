import React from "react";

const Contact = () => {
  const styles = {
    section: "h-screen snap-center",
  };

  return <div className={`${styles.section}`}>Contact</div>;
};

export default Contact;
